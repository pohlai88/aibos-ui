{
  "name": "aibos-ui-dev",
  "version": "1.0.0",
  "description": "Development workflow configuration for AIBOS UI package",
  "scripts": {
    "dev:start": "pnpm run dev",
    "dev:test": "pnpm run test:watch",
    "dev:lint": "pnpm run lint:fix",
    "dev:typecheck": "pnpm run typecheck:watch",
    "dev:full": "concurrently \"pnpm run dev:start\" \"pnpm run dev:test\" \"pnpm run dev:typecheck\"",
    "dev:clean": "pnpm run clean && pnpm install",
    "dev:reset": "pnpm run dev:clean && pnpm run validate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
