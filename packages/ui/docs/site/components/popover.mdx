# Popover

Floating content that appears above other elements to display additional information or actions.

## Import

```tsx
import { Popover, PopoverContent, PopoverTrigger } from '@aibos/ui';
```

## Basic Usage

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline">Open Popover</Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-2">
      <h4 className="font-medium">Popover Title</h4>
      <p className="text-semantic-muted-foreground text-sm">This is the popover content.</p>
    </div>
  </PopoverContent>
</Popover>
```

## Variants

### Default Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline">Default Popover</Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-2">
      <h4 className="font-medium">Default Popover</h4>
      <p className="text-semantic-muted-foreground text-sm">
        This is a default popover with basic content.
      </p>
    </div>
  </PopoverContent>
</Popover>
```

### With Form Content

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline">Add User</Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div>
        <h4 className="font-medium">Add New User</h4>
        <p className="text-semantic-muted-foreground text-sm">
          Fill out the form below to add a new user.
        </p>
      </div>
      <div className="space-y-3">
        <div>
          <label className="text-sm font-medium">Name</label>
          <Input placeholder="Enter name" />
        </div>
        <div>
          <label className="text-sm font-medium">Email</label>
          <Input type="email" placeholder="Enter email" />
        </div>
        <div className="flex justify-end space-x-2">
          <Button variant="outline" size="sm">
            Cancel
          </Button>
          <Button size="sm">Add User</Button>
        </div>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

### With Actions

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline" size="icon">
      <MoreHorizontalIcon className="h-4 w-4" />
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-2">
      <Button variant="ghost" className="w-full justify-start">
        <EditIcon className="mr-2 h-4 w-4" />
        Edit
      </Button>
      <Button variant="ghost" className="w-full justify-start">
        <CopyIcon className="mr-2 h-4 w-4" />
        Copy
      </Button>
      <Button variant="ghost" className="w-full justify-start">
        <ShareIcon className="mr-2 h-4 w-4" />
        Share
      </Button>
      <Button variant="ghost" className="text-semantic-destructive w-full justify-start">
        <TrashIcon className="mr-2 h-4 w-4" />
        Delete
      </Button>
    </div>
  </PopoverContent>
</Popover>
```

## Form Integration

### Controlled Popover

```tsx
function ControlledPopover() {
  const [open, setOpen] = useState(false);

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button variant="outline">Controlled Popover</Button>
      </PopoverTrigger>
      <PopoverContent>
        <div className="space-y-4">
          <div>
            <h4 className="font-medium">Controlled Popover</h4>
            <p className="text-semantic-muted-foreground text-sm">
              This popover is controlled by React state.
            </p>
          </div>
          <div className="flex justify-end space-x-2">
            <Button variant="outline" size="sm" onClick={() => setOpen(false)}>
              Close
            </Button>
          </div>
        </div>
      </PopoverContent>
    </Popover>
  );
}
```

### Dynamic Popover

```tsx
function DynamicPopover() {
  const [open, setOpen] = useState(false);
  const [items, setItems] = useState([
    { id: 1, name: 'Item 1', description: 'Description 1' },
    { id: 2, name: 'Item 2', description: 'Description 2' },
  ]);

  const addItem = () => {
    const newId = items.length + 1;
    setItems([
      ...items,
      {
        id: newId,
        name: `Item ${newId}`,
        description: `Description ${newId}`,
      },
    ]);
  };

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button variant="outline">Dynamic Popover</Button>
      </PopoverTrigger>
      <PopoverContent>
        <div className="space-y-4">
          <div className="flex items-center justify-between">
            <h4 className="font-medium">Dynamic Content</h4>
            <Button size="sm" onClick={addItem}>
              Add Item
            </Button>
          </div>
          <div className="space-y-2">
            {items.map((item) => (
              <div key={item.id} className="border-semantic-border rounded-md border p-2">
                <div className="font-medium">{item.name}</div>
                <div className="text-semantic-muted-foreground text-sm">{item.description}</div>
              </div>
            ))}
          </div>
        </div>
      </PopoverContent>
    </Popover>
  );
}
```

## Layout Examples

### User Profile Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="ghost" className="flex items-center space-x-2">
      <div className="bg-semantic-primary flex h-8 w-8 items-center justify-center rounded-full">
        <span className="text-sm font-medium text-white">JD</span>
      </div>
      <span>John Doe</span>
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div className="flex items-center space-x-3">
        <div className="bg-semantic-primary flex h-12 w-12 items-center justify-center rounded-full">
          <span className="text-lg font-medium text-white">JD</span>
        </div>
        <div>
          <div className="font-medium">John Doe</div>
          <div className="text-semantic-muted-foreground text-sm">john.doe@example.com</div>
        </div>
      </div>

      <div className="space-y-2">
        <Button variant="ghost" className="w-full justify-start">
          <UserIcon className="mr-2 h-4 w-4" />
          Profile
        </Button>
        <Button variant="ghost" className="w-full justify-start">
          <SettingsIcon className="mr-2 h-4 w-4" />
          Settings
        </Button>
        <Button variant="ghost" className="w-full justify-start">
          <HelpIcon className="mr-2 h-4 w-4" />
          Help
        </Button>
        <Button variant="ghost" className="text-semantic-destructive w-full justify-start">
          <LogOutIcon className="mr-2 h-4 w-4" />
          Sign Out
        </Button>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

### Filter Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline" className="flex items-center space-x-2">
      <FilterIcon className="h-4 w-4" />
      <span>Filters</span>
      <Badge variant="secondary">3</Badge>
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div>
        <h4 className="font-medium">Filter Options</h4>
        <p className="text-semantic-muted-foreground text-sm">Choose your filter criteria</p>
      </div>

      <div className="space-y-3">
        <div>
          <label className="text-sm font-medium">Category</label>
          <Select>
            <SelectTrigger>
              <SelectValue placeholder="Select category" />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="all">All Categories</SelectItem>
              <SelectItem value="technology">Technology</SelectItem>
              <SelectItem value="business">Business</SelectItem>
              <SelectItem value="design">Design</SelectItem>
            </SelectContent>
          </Select>
        </div>

        <div>
          <label className="text-sm font-medium">Date Range</label>
          <Select>
            <SelectTrigger>
              <SelectValue placeholder="Select date range" />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="today">Today</SelectItem>
              <SelectItem value="week">This Week</SelectItem>
              <SelectItem value="month">This Month</SelectItem>
              <SelectItem value="year">This Year</SelectItem>
            </SelectContent>
          </Select>
        </div>

        <div className="flex items-center space-x-2">
          <Checkbox id="featured" />
          <label htmlFor="featured" className="text-sm font-medium">
            Featured only
          </label>
        </div>
      </div>

      <div className="flex justify-end space-x-2">
        <Button variant="outline" size="sm">
          Clear
        </Button>
        <Button size="sm">Apply Filters</Button>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

### Notification Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="ghost" size="icon" className="relative">
      <BellIcon className="h-4 w-4" />
      <Badge className="absolute -right-1 -top-1">5</Badge>
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h4 className="font-medium">Notifications</h4>
        <Button variant="ghost" size="sm">
          Mark all read
        </Button>
      </div>

      <div className="space-y-3">
        <div className="flex items-start space-x-3">
          <div className="bg-semantic-primary mt-2 h-2 w-2 rounded-full" />
          <div className="flex-1">
            <div className="text-sm font-medium">New message</div>
            <div className="text-semantic-muted-foreground text-sm">
              You have a new message from John Doe
            </div>
            <div className="text-semantic-muted-foreground mt-1 text-xs">2 minutes ago</div>
          </div>
        </div>

        <div className="flex items-start space-x-3">
          <div className="bg-semantic-success mt-2 h-2 w-2 rounded-full" />
          <div className="flex-1">
            <div className="text-sm font-medium">Task completed</div>
            <div className="text-semantic-muted-foreground text-sm">
              Your task "Update documentation" has been completed
            </div>
            <div className="text-semantic-muted-foreground mt-1 text-xs">5 minutes ago</div>
          </div>
        </div>

        <div className="flex items-start space-x-3">
          <div className="bg-semantic-warning mt-2 h-2 w-2 rounded-full" />
          <div className="flex-1">
            <div className="text-sm font-medium">System alert</div>
            <div className="text-semantic-muted-foreground text-sm">
              System maintenance scheduled for tonight
            </div>
            <div className="text-semantic-muted-foreground mt-1 text-xs">1 hour ago</div>
          </div>
        </div>
      </div>

      <div className="text-center">
        <Button variant="ghost" size="sm">
          View all notifications
        </Button>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

### Search Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline" className="flex items-center space-x-2">
      <SearchIcon className="h-4 w-4" />
      <span>Search</span>
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div>
        <h4 className="font-medium">Search</h4>
        <p className="text-semantic-muted-foreground text-sm">Find what you're looking for</p>
      </div>

      <div className="space-y-3">
        <div>
          <Input placeholder="Search..." />
        </div>

        <div className="space-y-2">
          <div className="text-sm font-medium">Recent searches</div>
          <div className="space-y-1">
            <Button variant="ghost" className="w-full justify-start text-sm">
              <SearchIcon className="mr-2 h-3 w-3" />
              AIBOS ERP documentation
            </Button>
            <Button variant="ghost" className="w-full justify-start text-sm">
              <SearchIcon className="mr-2 h-3 w-3" />
              User management
            </Button>
            <Button variant="ghost" className="w-full justify-start text-sm">
              <SearchIcon className="mr-2 h-3 w-3" />
              API integration
            </Button>
          </div>
        </div>

        <div className="space-y-2">
          <div className="text-sm font-medium">Quick actions</div>
          <div className="grid grid-cols-2 gap-2">
            <Button variant="outline" size="sm">
              <FileTextIcon className="mr-1 h-3 w-3" />
              Documents
            </Button>
            <Button variant="outline" size="sm">
              <UsersIcon className="mr-1 h-3 w-3" />
              Users
            </Button>
            <Button variant="outline" size="sm">
              <SettingsIcon className="mr-1 h-3 w-3" />
              Settings
            </Button>
            <Button variant="outline" size="sm">
              <HelpIcon className="mr-1 h-3 w-3" />
              Help
            </Button>
          </div>
        </div>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

### Settings Popover

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline" size="icon">
      <SettingsIcon className="h-4 w-4" />
    </Button>
  </PopoverTrigger>
  <PopoverContent>
    <div className="space-y-4">
      <div>
        <h4 className="font-medium">Quick Settings</h4>
        <p className="text-semantic-muted-foreground text-sm">Adjust your preferences</p>
      </div>

      <div className="space-y-3">
        <div className="flex items-center justify-between">
          <div>
            <div className="text-sm font-medium">Dark Mode</div>
            <div className="text-semantic-muted-foreground text-sm">Switch to dark theme</div>
          </div>
          <Switch />
        </div>

        <div className="flex items-center justify-between">
          <div>
            <div className="text-sm font-medium">Notifications</div>
            <div className="text-semantic-muted-foreground text-sm">Receive notifications</div>
          </div>
          <Switch defaultChecked />
        </div>

        <div className="flex items-center justify-between">
          <div>
            <div className="text-sm font-medium">Auto Save</div>
            <div className="text-semantic-muted-foreground text-sm">Automatically save changes</div>
          </div>
          <Switch defaultChecked />
        </div>
      </div>

      <div className="border-semantic-border border-t pt-2">
        <Button variant="ghost" className="w-full justify-start">
          <SettingsIcon className="mr-2 h-4 w-4" />
          Advanced Settings
        </Button>
      </div>
    </div>
  </PopoverContent>
</Popover>
```

## API Reference

### Popover Props

| Prop           | Type                      | Default | Description                      |
| -------------- | ------------------------- | ------- | -------------------------------- |
| `open`         | `boolean`                 | -       | Controlled open state            |
| `defaultOpen`  | `boolean`                 | `false` | Uncontrolled open state          |
| `onOpenChange` | `(open: boolean) => void` | -       | Callback when open state changes |
| `className`    | `string`                  | -       | Additional CSS classes           |
| `children`     | `ReactNode`               | -       | Popover content                  |

### PopoverTrigger Props

| Prop        | Type        | Default | Description                               |
| ----------- | ----------- | ------- | ----------------------------------------- |
| `asChild`   | `boolean`   | `false` | Whether to merge props with child element |
| `className` | `string`    | -       | Additional CSS classes                    |
| `children`  | `ReactNode` | -       | Trigger content                           |

### PopoverContent Props

| Prop          | Type                                     | Default    | Description                         |
| ------------- | ---------------------------------------- | ---------- | ----------------------------------- |
| `side`        | `'top' \| 'right' \| 'bottom' \| 'left'` | `'bottom'` | Side of the trigger to show popover |
| `align`       | `'start' \| 'center' \| 'end'`           | `'center'` | Alignment of the popover            |
| `sideOffset`  | `number`                                 | `4`        | Distance from the trigger           |
| `alignOffset` | `number`                                 | `0`        | Alignment offset                    |
| `className`   | `string`                                 | -          | Additional CSS classes              |
| `children`    | `ReactNode`                              | -          | Popover content                     |

## Accessibility

- **Keyboard Navigation**: Full keyboard support for navigation
- **Screen Reader Support**: Proper ARIA attributes and announcements
- **Focus Management**: Clear focus indicators and proper tab order
- **Escape Key**: Closes popover when Escape is pressed
- **Click Outside**: Closes popover when clicking outside

## Best Practices

1. **Use clear triggers** - Provide obvious ways to open the popover
2. **Keep content focused** - Don't overload popovers with too much content
3. **Handle edge cases** - Manage positioning and overflow properly
4. **Provide clear actions** - Make it obvious how to close or complete actions
5. **Test with screen readers** - Ensure accessibility compliance
6. **Consider mobile devices** - Ensure popovers work well on touch devices

## Related Components

- [Tooltip](./tooltip.mdx) - Contextual information
- [Button](./button.mdx) - Interactive elements
- [Select](./select.mdx) - Dropdown selections
- [Menu](./menu.mdx) - Dropdown menus
