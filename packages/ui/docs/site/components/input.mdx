# Input

A versatile text input component with semantic tokens, accessibility features, and comprehensive styling options.

## Import

```tsx
import { Input } from '@aibos/ui';
```

## Basic Usage

```tsx
<Input placeholder="Enter your name" />
```

## Variants

### Default

```tsx
<Input placeholder="Default input" />
```

### With Label

```tsx
<div className="space-y-2">
  <label htmlFor="email" className="text-sm font-medium">
    Email
  </label>
  <Input id="email" type="email" placeholder="Enter your email" />
</div>
```

### With Error State

```tsx
<div className="space-y-2">
  <label htmlFor="password" className="text-sm font-medium">
    Password
  </label>
  <Input
    id="password"
    type="password"
    placeholder="Enter your password"
    className="border-semantic-destructive"
  />
  <p className="text-semantic-destructive text-sm">Password must be at least 8 characters</p>
</div>
```

### Disabled State

```tsx
<Input placeholder="Disabled input" disabled />
```

## Sizes

### Small

```tsx
<Input size="sm" placeholder="Small input" />
```

### Medium (Default)

```tsx
<Input size="md" placeholder="Medium input" />
```

### Large

```tsx
<Input size="lg" placeholder="Large input" />
```

## Types

### Text

```tsx
<Input type="text" placeholder="Text input" />
```

### Email

```tsx
<Input type="email" placeholder="Email input" />
```

### Password

```tsx
<Input type="password" placeholder="Password input" />
```

### Number

```tsx
<Input type="number" placeholder="Number input" />
```

### Search

```tsx
<Input type="search" placeholder="Search input" />
```

## With Icons

```tsx
<div className="relative">
  <Search className="text-semantic-muted-foreground absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2" />
  <Input className="pl-10" placeholder="Search..." />
</div>
```

## Form Integration

```tsx
import { useForm } from 'react-hook-form';

function ContactForm() {
  const { register, handleSubmit } = useForm();

  return (
    <form onSubmit={handleSubmit((data) => console.log(data))}>
      <div className="space-y-4">
        <div>
          <label htmlFor="name" className="text-sm font-medium">
            Name
          </label>
          <Input
            id="name"
            {...register('name', { required: true })}
            placeholder="Enter your name"
          />
        </div>
        <div>
          <label htmlFor="email" className="text-sm font-medium">
            Email
          </label>
          <Input
            id="email"
            type="email"
            {...register('email', { required: true })}
            placeholder="Enter your email"
          />
        </div>
        <Button type="submit">Submit</Button>
      </div>
    </form>
  );
}
```

## API Reference

### Props

| Prop           | Type                                             | Default  | Description                              |
| -------------- | ------------------------------------------------ | -------- | ---------------------------------------- |
| `type`         | `string`                                         | `"text"` | Input type (text, email, password, etc.) |
| `size`         | `"sm" \| "md" \| "lg"`                           | `"md"`   | Input size                               |
| `disabled`     | `boolean`                                        | `false`  | Whether the input is disabled            |
| `placeholder`  | `string`                                         | -        | Placeholder text                         |
| `value`        | `string`                                         | -        | Controlled value                         |
| `defaultValue` | `string`                                         | -        | Uncontrolled default value               |
| `onChange`     | `(event: ChangeEvent<HTMLInputElement>) => void` | -        | Change handler                           |
| `onFocus`      | `(event: FocusEvent<HTMLInputElement>) => void`  | -        | Focus handler                            |
| `onBlur`       | `(event: FocusEvent<HTMLInputElement>) => void`  | -        | Blur handler                             |
| `className`    | `string`                                         | -        | Additional CSS classes                   |
| `ref`          | `Ref<HTMLInputElement>`                          | -        | Ref to the input element                 |

## Accessibility

- **Keyboard Navigation**: Full keyboard support with Tab, Enter, and Escape
- **Screen Readers**: Proper labeling and ARIA attributes
- **Focus Management**: Visible focus indicators
- **Error Handling**: ARIA-describedby for error messages

## Best Practices

1. **Always provide labels** for form inputs
2. **Use appropriate input types** for better mobile experience
3. **Provide clear error messages** with proper ARIA attributes
4. **Use semantic tokens** for consistent styling
5. **Test with keyboard navigation** to ensure accessibility
