/**
 * Alert Wrapper - Enterprise Production Ready
 *
 * Thin wrapper over custom alert implementation with semantic tokens.
 * Provides accessible alert components for status messages and notifications.
 */

import { Alert, AlertTitle, AlertDescription, AlertClose } from '../primitives/alert';

// Re-export the primitive components as Radix wrappers
export {
  Alert,
  AlertTitle,
  AlertDescription,
  AlertClose,
};
