/**
 * Aspect Ratio Wrapper - Enterprise Production Ready
 *
 * Thin wrapper over @radix-ui/react-aspect-ratio with semantic tokens.
 * Provides accessible aspect ratio components with proper responsive behavior.
 */

import { AspectRatio } from '../primitives/aspect-ratio';

// Re-export the primitive component as Radix wrapper
export { AspectRatio };
