/**
 * Menu Icon - Enterprise Production Ready
 *
 * Hamburger menu icon with AIBOS aesthetic refinements.
 * Optimized for 24px display with sophisticated stroke weight.
 */

import { createIcon } from './_base';

export const MenuIcon = createIcon('MenuIcon', () => (
  <>
    <path d="M3 12h18" />
    <path d="M3 6h18" />
    <path d="M3 18h18" />
  </>
));
